spring:
  profiles:
    active: default
  jpa:
   database-platform: org.hibernate.dialect.H2Dialect
   hibernate:
      ddl-auto: create-drop
   properties:
      hibernate:
        default_schema: thymeleaf
        
server:
  port: 9001